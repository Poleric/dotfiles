dotfiles
========

Just a collection of dotfiles I use

## Usage

1. Install the `stow` package
    - On Arch run: 
      ```shell
      pacman -S stow
      ```
2. Stow the module by module
   ```shell
   stow -v --no-folding <module>
   ```
